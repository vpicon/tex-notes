% ----- PACKAGES -----

% Structure Packages
\usepackage[utf8]{inputenc}
\usepackage{multicol}   % multiple column file
\usepackage{fancyhdr}   % headers customization
\usepackage[a4paper, 
            top=2cm, 
            headsep=0.5cm,
            bottom=1cm, 
            hmargin={1cm,1cm}
            ]{geometry}  % document geometry
\usepackage[explicit]{titlesec}  % formatting titles
\usepackage{lipsum}      % lorem ipsum generator


% Utility Packages
\usepackage{hyperref}    % references of links
\usepackage{enumitem}    % advanced enumeration options and macros

\usepackage{lmodern}
\usepackage{slantsc}     % slanted text (to mix small caps and italics)

\usepackage{fancyvrb}    % for verbatim environments (minted)
\usepackage{fvextra}

\usepackage{amsmath}     % basic math package

\usepackage{tikz}        % drawings




% ----- DOCUMENT STRUCTURE -----

\pagenumbering{gobble}  % No page numbers

\pagestyle{fancy}       % Header format
\fancyhf{}
\lhead{\textbf{\titlename}}
\rhead{\date{\today}}

\titleformat{\section}[block]  % Section Titles format
  {\bf\Large\centering}{Chapter \thesection}{1em}{#1}


\setcounter{secnumdepth}{3}  % Allow subsubsections

% Align lengths
\setlength{\abovedisplayskip}{-5pt}%
\setlength{\belowdisplayskip}{-5pt}%
\setlength{\abovedisplayshortskip}{-5pt}%
\setlength{\belowdisplayshortskip}{-5pt}%


% ----- MACROS AND COMMANDS -----

\newcommand{\concept}[1]{\textit{#1}}


% ----- ENVIRONMENTS -----
\newenvironment{mysection}[1]
{%
    \newpage%
    \begin{multicols*}{2}%
    [\section{#1}]%
}
{%
\end{multicols*}
}



\newenvironment{smallitemize}
{ \begin{itemize}
    \setlength{\itemsep}{0pt}
    \setlength{\parskip}{0pt}
    \setlength{\parsep}{0pt}     }
{ \end{itemize}                  }
